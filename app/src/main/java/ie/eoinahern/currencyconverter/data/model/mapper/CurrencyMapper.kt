package ie.eoinahern.currencyconverter.data.model.mapper

import javax.inject.Singleton

object CurrencyMapper {

    fun mapToUIModel(inMap: Map<String, Double>): List<String> {
        return listOf("hello")
    }
}